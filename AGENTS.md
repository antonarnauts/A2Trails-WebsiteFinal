# Project Rules

- Never commit or modify images without explicit user permission.
- Always ensure image paths are GitHub Pages compliant (lowercase, no spaces/underscores).
- Use `getAssetPath` from `src/lib/utils.ts` for all asset references.
